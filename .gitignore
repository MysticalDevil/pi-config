# ── Cache ──
agent/mcp-cache.json
agent/mcp-npx-cache.json
context-mode/
.rumdl_cache/

# ── API keys & credentials (DO NOT COMMIT) ──
agent/auth.json
agent/models.json
agent/cliproxyapi-models.json
agent/settings.json
agent/trust.json
agent/memories.json
web-search.json
exa-usage.json

# ── Session history (full conversations, file contents, command output) ──
agent/sessions/

# ── Installed third-party packages (reinstall with `pi install`) ──
agent/npm/
agent/git/

# ── Dependencies ──
node_modules/

# ── Themes (third-party / auto-discovered) ──
agent/themes/

# ── Skills (third-party / auto-discovered) ──
agent/skills/

# ── Logs ──
*.log
